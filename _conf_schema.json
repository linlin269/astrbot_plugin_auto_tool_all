{
  "image_tool_name": {
    "description": "头像生图默认工具名",
    "type": "string",
    "default": "generate_image",
    "hint": "默认兼容 astrbot_plugin_selfie_image 的 generate_image；也可以填写其它插件暴露的生图工具名。"
  },
  "image_tool_candidates": {
    "description": "生图工具回退列表",
    "type": "list",
    "default": ["generate_image", "generate_selfie"],
    "hint": "首选工具不存在或停用时按顺序尝试。画“我/TA”等非机器人身份时，其中的 generate_selfie 会被自动跳过（它只能画机器人自己的形象）。"
  },
  "avatar_spec": {
    "description": "QQ头像尺寸",
    "type": "int",
    "default": 640,
    "hint": "qlogo 请求的头像规格，通常使用 640。"
  },
  "avatar_cache_ttl_minutes": {
    "description": "头像缓存时长（分钟）",
    "type": "int",
    "default": 360,
    "hint": "过期后重新下载头像。"
  },
  "max_reference_images": {
    "description": "单次最大参考图数量",
    "type": "int",
    "default": 4,
    "hint": "包含头像、消息图、引用图和跨工具接力图。"
  },
  "max_external_images": {
    "description": "单次最大外部图片下载数量",
    "type": "int",
    "default": 3
  },
  "external_image_max_mb": {
    "description": "单张外部图片大小上限（MB）",
    "type": "int",
    "default": 10
  },
  "tool_call_timeout_seconds": {
    "description": "工具调用超时（秒）",
    "type": "int",
    "default": 120
  },
  "tool_loop_max_steps": {
    "description": "单工具 Agent 最大步数",
    "type": "int",
    "default": 3
  },
  "allowed_tool_names": {
    "description": "允许通过万能入口调用的工具",
    "type": "list",
    "default": [],
    "hint": "留空表示允许所有 active 工具；填写后只允许列表中的工具。"
  },
  "blocked_tool_names": {
    "description": "禁止通过万能入口调用的工具",
    "type": "list",
    "default": [],
    "hint": "本插件自身的工具始终禁止递归调用。"
  },
  "enable_tool_listing": {
    "description": "启用工具列表查询",
    "type": "bool",
    "default": true
  },
  "deliver_media_base64": {
    "description": "媒体以 base64 发送",
    "type": "bool",
    "default": true,
    "hint": "工具返回中的图片/视频由插件下载后以 base64 消息发出（失败自动回退发 URL）；临时文件发送后立即清理。"
  },
  "media_image_max_mb": {
    "description": "base64 图片大小上限（MB）",
    "type": "int",
    "default": 10,
    "hint": "超过上限的图片回退为发送 URL。"
  },
  "media_video_max_mb": {
    "description": "base64 视频大小上限（MB）",
    "type": "int",
    "default": 100,
    "hint": "超过上限的视频回退为发送 URL。"
  },
  "media_max_count": {
    "description": "单次最多发送媒体数",
    "type": "int",
    "default": 5,
    "hint": "一次工具调用结果中最多转发的图片/视频数量，防止刷屏。"
  },
  "probe_enabled": {
    "description": "启用模型查看/测速触发",
    "type": "bool",
    "default": true,
    "hint": "监听“看看里面有什么模型/帮我测试一下里面的模型”类话语并自动调用 OpenAI 兼容接口。"
  },
  "probe_concurrency": {
    "description": "模型测速并发数",
    "type": "int",
    "default": 3,
    "hint": "同时对多少个模型发送测试消息（1-10）。"
  },
  "probe_timeout_seconds": {
    "description": "单模型测速超时（秒）",
    "type": "int",
    "default": 10,
    "hint": "每个模型测试请求的超时时间，超时/报错/空回复都视为不可用。"
  },
  "probe_memory_ttl_minutes": {
    "description": "url/key 记忆时长（分钟）",
    "type": "int",
    "default": 30,
    "hint": "分开多条消息发送 url 和 key 时的配对记忆时长；仅存内存，重启即失，成功使用后 key 立即丢弃。"
  },
  "debug_logging": {
    "description": "启用调试日志",
    "type": "bool",
    "default": false,
    "hint": "开启后输出 avatar_draw 的身份解析结果、QQ 号、头像 URL 与工具选择细节，便于排查“画错人”问题。"
  }
}
